# Writing

`/draft-letter` reads `../identity/voice.md` first, then picks a template here.
Templates use `<angle brackets>` for slots. Keep finished drafts worth reusing in
`letters/sent/` or `emails/sent/` so the voice file gets richer over time.
